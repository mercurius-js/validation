'use strict'

// const { MER_VALIDATION_ERR_INVALID_OPTS } = require('./errors')

// TODO: correct these
function validateOpts (opts) {
  // if (typeof opts.applyValidation !== 'function') {
  //   throw new MER_VALIDATION_ERR_INVALID_OPTS('opts.applyValidation must be a function.')
  // }
  // if (typeof opts.validationDirective !== 'string') {
  //   throw new MER_VALIDATION_ERR_INVALID_OPTS('opts.validationDirective must be a string.')
  // }
}

module.exports.validateOpts = validateOpts
